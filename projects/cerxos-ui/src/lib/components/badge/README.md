# Cxs Badge

Token-driven badge component for Cerxos UI.

## Usage

```html
<cxs-badge>Beta</cxs-badge>
<cxs-badge variant="danger">Blocked</cxs-badge>
```

## Inputs

- `variant`: `primary` | `neutral` | `danger` (default: `primary`)
- `size`: `sm` | `md` (default: `md`)
