# Cxs Theme Directive

Applies the Cerxos theme class to any container and optionally follows system preference.

## Usage

```html
<main [cxsTheme]="mode"></main>
```

## Inputs

- `cxsTheme`: `light` | `dark` | `system` (default: `light`)
- `cxsThemeDarkClass`: string (default: `cxs-theme-dark`)
